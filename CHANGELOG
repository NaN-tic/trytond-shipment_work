* Ensure that not timesheet lines are created until work exists
* Enforce that shipment hours product is salable
* Add default values for payment term and for warehouse
* Only allow to modify invoice relative fields to Shipment Work Administration users
* Allow to invoice shipment hours
* Fix copy defintion and allow to delete shipments with lines
* Allow to specify multiple employees per shipment work
* Add total_hours field
* Allow to relate stock moves with a shipment work and make them when checked
* Add invoice method to shipment and products
* Add code field and define a sequence to fill it
* A shimpment work should be assigned only to one employee
* Add planned hours field

Version 3.4.0 - 2014-11-03
* Initial release
